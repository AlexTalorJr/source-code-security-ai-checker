<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}Dashboard{% endblock %} - Security Scanner</title>
  {% block head_extra %}{% endblock %}
  <style>
    :root {
      --color-bg: #ffffff;
      --color-surface: #f8f9fa;
      --color-accent: #0d6efd;
      --color-destructive: #dc3545;
      --color-success: #28a745;
      --color-border: #dee2e6;
      --color-text: #212529;
      --color-text-secondary: #6c757d;
      --font-system: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      --font-mono: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-system);
      font-size: 14px;
      font-weight: 400;
      line-height: 1.5;
      color: var(--color-text);
      background: var(--color-bg);
    }

    /* Nav Bar */
    .navbar {
      display: flex;
      align-items: center;
      padding: 0 24px;
      height: 48px;
      background-color: var(--color-surface);
      border-bottom: 1px solid var(--color-border);
    }

    .navbar__links {
      display: flex;
      align-items: center;
      gap: 24px;
      flex: 1;
    }

    .navbar__link {
      text-decoration: none;
      color: var(--color-text);
      font-size: 14px;
      font-weight: 400;
      padding: 12px 0;
      border-bottom: 2px solid transparent;
    }

    .navbar__link:hover {
      color: var(--color-accent);
    }

    .navbar__link--active {
      color: var(--color-accent);
      border-bottom-color: var(--color-accent);
    }

    .navbar__right {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-left: auto;
    }

    .navbar__user {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-right: 16px;
    }

    .navbar__role-badge {
      font-size: 10px;
      padding: 4px 8px;
    }

    .navbar__logout {
      text-decoration: none;
      color: var(--color-text-secondary);
      font-size: 14px;
    }

    .navbar__logout:hover {
      color: var(--color-destructive);
    }

    /* Content Area */
    .content {
      max-width: 1200px;
      margin: 0 auto;
      padding: 24px;
    }

    .page-heading {
      font-size: 20px;
      font-weight: 600;
      line-height: 1.2;
      margin-bottom: 24px;
    }

    /* Utility classes */
    .badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      color: #ffffff;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      line-height: 1.4;
    }

    .btn {
      display: inline-block;
      padding: 8px 16px;
      border: none;
      border-radius: 4px;
      font-size: 14px;
      font-weight: 400;
      cursor: pointer;
      text-decoration: none;
      line-height: 1.5;
    }

    .btn--accent {
      background-color: var(--color-accent);
      color: #ffffff;
    }

    .btn--accent:hover {
      opacity: 0.9;
    }

    .btn--destructive {
      background-color: var(--color-destructive);
      color: #ffffff;
    }

    .btn--small {
      padding: 4px 8px;
      font-size: 12px;
    }

    /* Form classes */
    .form-group {
      margin-bottom: 16px;
    }

    .form-input {
      width: 100%;
      padding: 8px;
      border: 1px solid var(--color-border);
      border-radius: 4px;
      font-size: 14px;
    }

    .form-input:focus {
      outline: 2px solid var(--color-accent);
      border-color: var(--color-accent);
    }

    .form-select {
      width: 100%;
      padding: 8px;
      border: 1px solid var(--color-border);
      border-radius: 4px;
      font-size: 14px;
      appearance: auto;
      background: var(--color-bg);
    }

    .form-select:focus {
      outline: 2px solid var(--color-accent);
      border-color: var(--color-accent);
    }

    /* Token display */
    .token-value {
      font-family: var(--font-mono);
      background: var(--color-surface);
      padding: 8px 16px;
      border: 1px solid var(--color-border);
      border-radius: 4px;
      word-break: break-all;
    }

    .token-masked {
      font-family: var(--font-mono);
      color: var(--color-text-secondary);
    }

    /* Alerts */
    .error-alert {
      background: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
      border-left: 4px solid var(--color-destructive);
      border-radius: 4px;
      padding: 8px 12px;
      font-size: 14px;
      margin-bottom: 16px;
    }

    .success-alert {
      background: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
      border-left: 4px solid var(--color-success);
      border-radius: 4px;
      padding: 8px 16px;
      font-size: 14px;
      margin-bottom: 16px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 8px;
      text-align: left;
      border-bottom: 1px solid var(--color-border);
    }

    th {
      font-size: 12px;
      font-weight: 600;
      color: var(--color-text-secondary);
      background-color: var(--color-surface);
    }

    tr:nth-child(even) {
      background-color: var(--color-surface);
    }

    .empty-state {
      text-align: center;
      padding: 48px 24px;
      color: var(--color-text-secondary);
    }

    .empty-state h2 {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--color-text);
    }

    .empty-state p {
      font-size: 14px;
    }

    {% block extra_styles %}{% endblock %}
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar__links">
      <a href="/dashboard/history" class="navbar__link{% if active_page == 'history' %} navbar__link--active{% endif %}">Scan History</a>
      <a href="/dashboard/trends" class="navbar__link{% if active_page == 'trends' %} navbar__link--active{% endif %}">Trends</a>
      {% if user and user.role == "admin" %}
      <a href="/dashboard/users" class="navbar__link{% if active_page == 'users' %} navbar__link--active{% endif %}">Users</a>
      {% endif %}
      {% if user %}
      <a href="/dashboard/tokens" class="navbar__link{% if active_page == 'tokens' %} navbar__link--active{% endif %}">API Tokens</a>
      {% endif %}
    </div>
    <div class="navbar__right">
      {% if user %}
      <div class="navbar__user">
        <span style="font-size: 14px;">{{ user.username }}</span>
        <span class="badge navbar__role-badge" style="background-color: {% if user.role == 'admin' %}#0d6efd{% elif user.role == 'scanner' %}#6c757d{% else %}#28a745{% endif %};">{{ user.role|upper }}</span>
      </div>
      <a href="/dashboard/logout" class="navbar__logout">Log Out</a>
      {% endif %}
    </div>
  </nav>

  <div class="content">
    {% block content %}{% endblock %}
  </div>
</body>
</html>
