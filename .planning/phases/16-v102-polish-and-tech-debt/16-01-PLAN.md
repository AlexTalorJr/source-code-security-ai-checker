---
phase: 16-v102-polish-and-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scanner/main.py
  - src/scanner/dashboard/router.py
  - src/scanner/dashboard/templates/history.html.j2
  - docs/en/architecture.md
  - docs/en/devops-guide.md
  - docs/en/transfer-guide.md
  - docs/ru/architecture.md
  - docs/ru/devops-guide.md
  - docs/ru/transfer-guide.md
  - docs/fr/architecture.md
  - docs/fr/devops-guide.md
  - docs/fr/transfer-guide.md
  - docs/es/architecture.md
  - docs/es/devops-guide.md
  - docs/es/transfer-guide.md
  - docs/it/architecture.md
  - docs/it/devops-guide.md
  - docs/it/transfer-guide.md
autonomous: true
requirements:
  - DAST-02
gap_closure: true

must_haves:
  truths:
    - "Dashboard scan form has a target_url input field for DAST scans"
    - "target_url column migration exists in _apply_schema_updates inline dict"
    - "No X-API-Key references remain in any docs/ files across all 5 languages"
  artifacts:
    - path: "src/scanner/main.py"
      provides: "target_url column in inline migration dict"
      contains: "target_url"
    - path: "src/scanner/dashboard/templates/history.html.j2"
      provides: "target_url input field in scan form"
      contains: "target_url"
    - path: "src/scanner/dashboard/router.py"
      provides: "target_url Form parameter in start_scan handler"
      contains: "target_url"
  key_links:
    - from: "src/scanner/dashboard/templates/history.html.j2"
      to: "src/scanner/dashboard/router.py"
      via: "form POST name='target_url' -> target_url: str = Form()"
      pattern: "name=\"target_url\""
    - from: "src/scanner/dashboard/router.py"
      to: "src/scanner/models/scan.py"
      via: "ScanResult(target_url=target_url)"
      pattern: "target_url=target_url"
---

<objective>
Close three tech debt items from the v1.0.2 milestone audit: add target_url to the dashboard scan form (completing the DAST dashboard flow), add target_url to the inline schema migration dict, and replace all stale X-API-Key references in docs with Bearer token auth.

Purpose: Finalize v1.0.2 milestone by closing all identified gaps.
Output: Updated main.py, dashboard router/template, and 15 doc files.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0.2-MILESTONE-AUDIT.md

<interfaces>
<!-- ScanResult model from src/scanner/models/scan.py -->
target_url = Column(String(500), nullable=True)  # DAST target URL

<!-- Current start_scan handler signature (src/scanner/dashboard/router.py lines 639-647) -->
@router.post("/start-scan")
async def start_scan(
    request: Request,
    path: str = Form(default=""),
    repo_url: str = Form(default=""),
    branch: str = Form(default=""),
    skip_ai: str = Form(default=""),
    profile: str = Form(default=""),
):

<!-- Current _apply_schema_updates migrations dict (src/scanner/main.py lines 30-37) -->
migrations = {
    "ai_cost_usd": "ALTER TABLE scans ADD COLUMN ai_cost_usd FLOAT",
    "skip_ai": "ALTER TABLE scans ADD COLUMN skip_ai BOOLEAN NOT NULL DEFAULT 0",
    "scanner_version": "ALTER TABLE scans ADD COLUMN scanner_version VARCHAR(20)",
    "tool_versions": "ALTER TABLE scans ADD COLUMN tool_versions TEXT",
    "created_at": "ALTER TABLE scans ADD COLUMN created_at DATETIME DEFAULT (datetime('now'))",
    "profile_name": "ALTER TABLE scans ADD COLUMN profile_name VARCHAR(200)",
}

<!-- Current scan form fields in history.html.j2 (lines 74-111) -->
<!-- Has: path, repo_url, branch, skip_ai, profile -->
<!-- Missing: target_url -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add target_url migration and dashboard DAST form support</name>
  <files>src/scanner/main.py, src/scanner/dashboard/router.py, src/scanner/dashboard/templates/history.html.j2</files>
  <read_first>
    - src/scanner/main.py (lines 20-42 — _apply_schema_updates function)
    - src/scanner/dashboard/router.py (lines 639-672 — start_scan handler)
    - src/scanner/dashboard/templates/history.html.j2 (lines 74-111 — scan form)
    - src/scanner/models/scan.py (line 19 — target_url column definition)
  </read_first>
  <action>
THREE changes to three files:

1. **src/scanner/main.py** — Add target_url to the migrations dict (line ~37, after the "profile_name" entry):
   ```python
   "target_url": "ALTER TABLE scans ADD COLUMN target_url VARCHAR(500)",
   ```

2. **src/scanner/dashboard/router.py** — Add target_url parameter to start_scan handler:
   - Add `target_url: str = Form(default=""),` after the `repo_url` parameter (after line 643)
   - Add `target_url=target_url or None,` to the ScanResult constructor (after line 657, alongside target_path, repo_url, branch)
   - The three-way validation (target_url exclusive with path/repo_url per Phase 13-03 decision) is enforced at the API layer; the dashboard form is a convenience — user fills in EITHER path/repo_url OR target_url

3. **src/scanner/dashboard/templates/history.html.j2** — Add target_url input field to the scan form:
   - Add a new form-group in the first form-row (after the branch field, before the closing div on line 88):
   ```html
   <div class="form-group">
     <label for="target_url">Target URL (DAST)</label>
     <input type="text" id="target_url" name="target_url" placeholder="https://example.com">
   </div>
   ```
   This adds it alongside path, repo_url, and branch in the same row.
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && grep -q '"target_url":.*ALTER TABLE' src/scanner/main.py && grep -q 'target_url.*Form' src/scanner/dashboard/router.py && grep -q 'name="target_url"' src/scanner/dashboard/templates/history.html.j2 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"target_url"' src/scanner/main.py returns the ALTER TABLE line in _apply_schema_updates migrations dict
    - grep 'target_url: str = Form' src/scanner/dashboard/router.py returns the new Form parameter
    - grep 'target_url=target_url' src/scanner/dashboard/router.py returns the ScanResult constructor line
    - grep 'name="target_url"' src/scanner/dashboard/templates/history.html.j2 returns the input field
  </acceptance_criteria>
  <done>Dashboard scan form has target_url input, start_scan handler passes it to ScanResult, and inline migration dict includes target_url column</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale X-API-Key references with Bearer token auth in all docs</name>
  <files>docs/en/architecture.md, docs/en/devops-guide.md, docs/en/transfer-guide.md, docs/ru/architecture.md, docs/ru/devops-guide.md, docs/ru/transfer-guide.md, docs/fr/architecture.md, docs/fr/devops-guide.md, docs/fr/transfer-guide.md, docs/es/architecture.md, docs/es/devops-guide.md, docs/es/transfer-guide.md, docs/it/architecture.md, docs/it/devops-guide.md, docs/it/transfer-guide.md</files>
  <read_first>
    - docs/en/architecture.md (line 170 — stale X-API-Key auth description)
    - docs/en/devops-guide.md (lines 128-142 — Jenkins example with X-API-Key)
    - docs/en/transfer-guide.md (lines 127-133 — curl example with X-API-Key)
    - docs/en/admin-guide.md (lines 165-167 — correct Bearer token example for reference)
    - docs/en/api.md (lines 10-15 — correct Bearer auth description for reference)
  </read_first>
  <action>
Replace all 15 X-API-Key occurrences across 15 files (3 file types x 5 languages). Each file type has a specific replacement pattern:

**architecture.md (line 170 in all 5 languages):**
Replace the entire line. The current line describes X-API-Key auth. Replace with Bearer token auth description in each language:
- EN: `- **Bearer token authentication** -- all API endpoints (except /api/health) require a valid Bearer token in the Authorization header; tokens are generated per-user from the dashboard`
- RU: `- **Аутентификация через Bearer-токен** -- все API-эндпоинты (кроме /api/health) требуют валидный Bearer-токен в заголовке Authorization; токены генерируются для каждого пользователя через панель управления`
- FR: `- **Authentification par jeton Bearer** -- tous les endpoints API (sauf /api/health) requierent un jeton Bearer valide dans le header Authorization ; les jetons sont generes par utilisateur depuis le tableau de bord`
- ES: `- **Autenticacion mediante token Bearer** -- todos los endpoints de la API (excepto /api/health) requieren un token Bearer valido en el encabezado Authorization; los tokens se generan por usuario desde el panel de control`
- IT: `- **Autenticazione tramite token Bearer** -- tutti gli endpoint API (tranne /api/health) richiedono un token Bearer valido nell'header Authorization; i token vengono generati per utente dalla dashboard`

**devops-guide.md (line 131-133 depending on language):**
Replace the Jenkins customHeaders line. Change from:
```
customHeaders: [[name: 'X-API-Key', value: "${SCANNER_API_KEY}"]],
```
To:
```
customHeaders: [[name: 'Authorization', value: "Bearer ${SCANNER_API_TOKEN}"]],
```
This is the same in all 5 languages (it is a code block).

**transfer-guide.md (line 130 in all 5 languages):**
Replace the curl header. Change from:
```
      -H "X-API-Key: your-key" \
```
To:
```
      -H "Authorization: Bearer nvsec_your_token" \
```
This is the same in all 5 languages (it is a code block).
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && count=$(grep -r "X-API-Key" docs/ | wc -l) && echo "X-API-Key occurrences: $count" && test "$count" -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "X-API-Key" docs/ returns zero matches
    - grep "Bearer" docs/en/architecture.md returns the new auth description on line 170
    - grep "Bearer" docs/en/devops-guide.md returns the new Authorization header in Jenkins example
    - grep "Bearer" docs/en/transfer-guide.md returns the new curl header
    - Same patterns hold for ru, fr, es, it language directories
  </acceptance_criteria>
  <done>All 15 X-API-Key references replaced with Bearer token auth across all 5 language directories; zero X-API-Key occurrences remain in docs/</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end integration</name>
  <files></files>
  <read_first>
    - src/scanner/main.py (verify target_url in migrations dict)
    - src/scanner/dashboard/router.py (verify target_url in start_scan)
    - src/scanner/dashboard/templates/history.html.j2 (verify target_url input)
  </read_first>
  <action>
Run the project tests to ensure nothing is broken by the changes. Then run targeted verification:

1. Run existing test suite: `cd /home/alex/naveksoft-security && python -m pytest tests/ -x -q --timeout=60`
2. Verify migration dict has target_url: `python -c "import ast; src=open('src/scanner/main.py').read(); assert 'target_url' in src"`
3. Verify zero X-API-Key in docs: `test $(grep -r 'X-API-Key' docs/ | wc -l) -eq 0`
4. Verify form has target_url input: `grep -c 'name=\"target_url\"' src/scanner/dashboard/templates/history.html.j2` returns 1
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && python -m pytest tests/ -x -q --timeout=60 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All existing tests pass (zero failures)
    - grep -r "X-API-Key" docs/ returns nothing
    - grep "target_url" src/scanner/main.py shows migration entry
    - grep 'name="target_url"' src/scanner/dashboard/templates/history.html.j2 shows form input
    - grep "target_url.*Form" src/scanner/dashboard/router.py shows handler parameter
  </acceptance_criteria>
  <done>All three gap closure items verified: migration exists, dashboard form has DAST field, docs are clean of X-API-Key</done>
</task>

</tasks>

<verification>
Phase 16 gap closure is complete when:
1. `grep -c "target_url" src/scanner/main.py` shows the column in _apply_schema_updates migrations
2. `grep -c 'name="target_url"' src/scanner/dashboard/templates/history.html.j2` returns 1
3. `grep -c "target_url.*Form" src/scanner/dashboard/router.py` returns 1
4. `grep -rc "X-API-Key" docs/` returns 0 for every file
5. `python -m pytest tests/ -x -q` passes with no failures
</verification>

<success_criteria>
- Dashboard scan form includes target_url input for DAST scans
- start_scan handler accepts target_url and passes it to ScanResult
- _apply_schema_updates includes target_url VARCHAR(500) migration
- Zero X-API-Key references remain in docs/ (all 5 languages, all 3 file types)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-v102-polish-and-tech-debt/16-01-SUMMARY.md`
</output>
