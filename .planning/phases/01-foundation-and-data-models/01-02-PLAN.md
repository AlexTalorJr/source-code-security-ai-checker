---
phase: 01-foundation-and-data-models
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/scanner/models/__init__.py
  - src/scanner/models/base.py
  - src/scanner/models/finding.py
  - src/scanner/models/scan.py
  - src/scanner/db/__init__.py
  - src/scanner/db/session.py
  - src/scanner/api/__init__.py
  - src/scanner/api/router.py
  - src/scanner/api/health.py
  - src/scanner/main.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/.gitkeep
  - tests/test_models.py
  - tests/test_health.py
autonomous: true
requirements:
  - SCAN-02
  - API-04
  - INFRA-05

must_haves:
  truths:
    - "SQLite database is created with WAL mode enabled on every connection"
    - "Finding and ScanResult ORM models persist data to SQLite"
    - "GET /api/health returns 200 with status, version, uptime_seconds, and database fields"
    - "Database persists data across session recreation"
    - "Foreign keys are enforced in SQLite"
  artifacts:
    - path: "src/scanner/models/finding.py"
      provides: "Finding ORM model"
      contains: "class Finding(Base)"
    - path: "src/scanner/models/scan.py"
      provides: "ScanResult ORM model"
      contains: "class ScanResult(Base)"
    - path: "src/scanner/db/session.py"
      provides: "Async engine factory with WAL mode"
      contains: "_set_sqlite_pragmas"
    - path: "src/scanner/main.py"
      provides: "FastAPI app factory with lifespan"
      contains: "def create_app"
    - path: "src/scanner/api/health.py"
      provides: "Health check endpoint"
      contains: 'router.get("/health"'
  key_links:
    - from: "src/scanner/models/finding.py"
      to: "src/scanner/models/scan.py"
      via: "ForeignKey relationship"
      pattern: 'ForeignKey.*scans\\.id'
    - from: "src/scanner/main.py"
      to: "src/scanner/db/session.py"
      via: "Engine creation in lifespan"
      pattern: "create_engine.*db_path"
    - from: "src/scanner/main.py"
      to: "src/scanner/api/router.py"
      via: "Router inclusion"
      pattern: "include_router.*api_router"
    - from: "src/scanner/api/health.py"
      to: "src/scanner/db/session.py"
      via: "DB connectivity check"
      pattern: "session.*execute.*SELECT 1"
---

<objective>
Create SQLAlchemy ORM models with async SQLite persistence (WAL mode), FastAPI application with health endpoint, and Alembic migration skeleton.

Purpose: Wire the Pydantic schemas from Plan 01 into a running database and API server, establishing the persistence and API layers all future phases extend.
Output: Running FastAPI app with health endpoint, SQLite DB with WAL mode, ORM models matching the Pydantic schemas.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-models/01-RESEARCH.md
@.planning/phases/01-foundation-and-data-models/01-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From src/scanner/schemas/severity.py:
```python
class Severity(IntEnum):
    CRITICAL = 5
    HIGH = 4
    MEDIUM = 3
    LOW = 2
    INFO = 1
```

From src/scanner/config.py:
```python
class ScannerSettings(BaseSettings):
    host: str = "0.0.0.0"
    port: int = 8000
    db_path: str = "/data/scanner.db"
    api_key: str = ""
    claude_api_key: str = ""
    # ... plus env_prefix="SCANNER_", YamlConfigSettingsSource
```

From src/scanner/schemas/finding.py:
```python
class FindingSchema(BaseModel):
    fingerprint: str
    tool: str
    rule_id: str
    file_path: str
    severity: Severity
    title: str
    # ... plus optional fields
```

From src/scanner/schemas/scan.py:
```python
class ScanResultSchema(BaseModel):
    id: int | None = None
    status: str = "pending"
    total_findings: int = 0
    # ... plus counts, timestamps, metadata
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLAlchemy ORM models, DB session with WAL mode, and Alembic skeleton</name>
  <files>
    src/scanner/models/__init__.py
    src/scanner/models/base.py
    src/scanner/models/finding.py
    src/scanner/models/scan.py
    src/scanner/db/__init__.py
    src/scanner/db/session.py
    alembic.ini
    alembic/env.py
    alembic/script.py.mako
    alembic/versions/.gitkeep
    tests/test_models.py
  </files>
  <read_first>
    .planning/phases/01-foundation-and-data-models/01-RESEARCH.md
    src/scanner/schemas/severity.py
    src/scanner/schemas/finding.py
    src/scanner/schemas/scan.py
    src/scanner/config.py
  </read_first>
  <behavior>
    - test_severity_enum: Severity enum imported in finding model, values 1-5
    - test_finding_model_columns: Finding model has columns: id, scan_id, fingerprint, tool, rule_id, file_path, line_start, line_end, snippet, severity, title, description, recommendation, ai_analysis, ai_fix_suggestion, false_positive, created_at
    - test_scan_model_columns: ScanResult model has columns: id, target_path, repo_url, branch, commit_hash, status, started_at, completed_at, duration_seconds, total_findings, critical_count, high_count, medium_count, low_count, info_count, gate_passed, scanner_version, tool_versions, error_message, created_at
    - test_finding_scan_foreign_key: Finding.scan_id references scans.id
    - test_sqlite_wal_mode: After engine creation, PRAGMA journal_mode returns "wal"
    - test_sqlite_foreign_keys: After engine creation, PRAGMA foreign_keys returns 1
    - test_create_and_read_scan: Insert ScanResult, read back, verify fields match
    - test_create_and_read_finding: Insert Finding with scan_id, read back, verify fields
    - test_db_persistence: Insert data, dispose engine, create new engine on same file, data still present
  </behavior>
  <action>
    1. Create `src/scanner/models/base.py`:
       ```python
       from sqlalchemy.orm import DeclarativeBase
       class Base(DeclarativeBase):
           pass
       ```

    2. Create `src/scanner/models/scan.py` with class ScanResult(Base):
       - __tablename__ = "scans"
       - Columns exactly as in RESEARCH.md code example:
         id (Integer, primary_key, autoincrement), target_path (String 500, nullable), repo_url (String 500, nullable), branch (String 200, nullable), commit_hash (String 40, nullable), status (String 20, not null, default="pending"), started_at (DateTime, nullable), completed_at (DateTime, nullable), duration_seconds (Float, nullable), total_findings (Integer, default=0), critical_count (Integer, default=0), high_count (Integer, default=0), medium_count (Integer, default=0), low_count (Integer, default=0), info_count (Integer, default=0), gate_passed (Integer, nullable), scanner_version (String 20, nullable), tool_versions (Text, nullable), error_message (Text, nullable), created_at (DateTime, default=datetime.utcnow)
       - Add relationship: `findings = relationship("Finding", back_populates="scan", cascade="all, delete-orphan")`

    3. Create `src/scanner/models/finding.py` with class Finding(Base):
       - __tablename__ = "findings"
       - Columns exactly as in RESEARCH.md:
         id (Integer, primary_key, autoincrement), scan_id (Integer, ForeignKey("scans.id"), not null, index), fingerprint (String 64, not null, index), tool (String 50, not null), rule_id (String 200, not null), file_path (String 500, not null), line_start (Integer, nullable), line_end (Integer, nullable), snippet (Text, nullable), severity (Integer, not null), title (String 500, not null), description (Text, nullable), recommendation (Text, nullable), ai_analysis (Text, nullable), ai_fix_suggestion (Text, nullable), false_positive (Integer, default=0), created_at (DateTime, default=datetime.utcnow)
       - Add relationship: `scan = relationship("ScanResult", back_populates="findings")`

    4. Create `src/scanner/models/__init__.py` exporting Base, Finding, ScanResult

    5. Create `src/scanner/db/__init__.py` (empty)

    6. Create `src/scanner/db/session.py`:
       - `_set_sqlite_pragmas(dbapi_conn, connection_record)` function that executes:
         PRAGMA journal_mode=WAL
         PRAGMA synchronous=NORMAL
         PRAGMA foreign_keys=ON
       - `create_engine(db_path: str)` returning AsyncEngine with sqlite+aiosqlite URL, echo=False, event.listen for pragmas
       - `create_session_factory(engine)` returning async_sessionmaker[AsyncSession]
       - Exactly following RESEARCH.md Pattern 4

    7. Create Alembic skeleton:
       - `alembic.ini` with sqlalchemy.url = sqlite+aiosqlite:///data/scanner.db (overridden in env.py)
       - `alembic/env.py` with async support: import from scanner.models.base import Base as target_metadata, use run_async for migrations
       - `alembic/script.py.mako` (standard template)
       - `alembic/versions/.gitkeep`

    8. Create `tests/test_models.py` with all behavior tests.
       - Use a tmp_path fixture for test DB: `db_path = str(tmp_path / "test.db")`
       - Create engine, run Base.metadata.create_all in async setup
       - Tests use async sessions

    TDD flow: Write tests (RED), implement models + session (GREEN).
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && pytest tests/test_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/models/base.py contains "class Base(DeclarativeBase)"
    - src/scanner/models/finding.py contains "class Finding(Base)"
    - src/scanner/models/finding.py contains '__tablename__ = "findings"'
    - src/scanner/models/finding.py contains 'ForeignKey("scans.id")'
    - src/scanner/models/finding.py contains "fingerprint = Column(String(64)"
    - src/scanner/models/scan.py contains "class ScanResult(Base)"
    - src/scanner/models/scan.py contains '__tablename__ = "scans"'
    - src/scanner/db/session.py contains "def _set_sqlite_pragmas"
    - src/scanner/db/session.py contains "PRAGMA journal_mode=WAL"
    - src/scanner/db/session.py contains "PRAGMA foreign_keys=ON"
    - src/scanner/db/session.py contains "create_async_engine"
    - src/scanner/db/session.py contains "async_sessionmaker"
    - alembic.ini exists
    - alembic/env.py contains "target_metadata"
    - `pytest tests/test_models.py -x` exits 0
  </acceptance_criteria>
  <done>
    SQLAlchemy ORM models for Finding and ScanResult persist to SQLite with WAL mode. Foreign key between findings and scans enforced. Data persists across engine recreations. Alembic skeleton ready for future migrations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastAPI application factory with health endpoint</name>
  <files>
    src/scanner/api/__init__.py
    src/scanner/api/router.py
    src/scanner/api/health.py
    src/scanner/main.py
    tests/test_health.py
  </files>
  <read_first>
    .planning/phases/01-foundation-and-data-models/01-RESEARCH.md
    src/scanner/config.py
    src/scanner/db/session.py
    src/scanner/models/base.py
  </read_first>
  <behavior>
    - test_health_endpoint_200: GET /api/health returns 200
    - test_health_response_fields: Response JSON has keys: status, version, uptime_seconds, database
    - test_health_status_healthy: When DB is accessible, status == "healthy" and database == "ok"
    - test_health_version: version == "0.1.0"
    - test_health_uptime_positive: uptime_seconds >= 0
    - test_app_creates_tables: After app startup, "scans" and "findings" tables exist in the DB
  </behavior>
  <action>
    1. Create `src/scanner/api/__init__.py` (empty)

    2. Create `src/scanner/api/health.py`:
       - Import time, APIRouter, Request, BaseModel
       - Module-level `_start_time = time.time()`
       - `class HealthResponse(BaseModel)` with fields: status (str), version (str), uptime_seconds (float), database (str)
       - `router = APIRouter()`
       - `@router.get("/health", response_model=HealthResponse)` async endpoint that:
         - Tries to execute "SELECT 1" via request.app.state.session_factory
         - Sets db_status = "ok" on success, "error" on exception
         - Returns HealthResponse with status="healthy" if db ok else "degraded", version="0.1.0", uptime from _start_time, database=db_status
       - Use `from sqlalchemy import text` and `await session.execute(text("SELECT 1"))` (not raw string)

    3. Create `src/scanner/api/router.py`:
       ```python
       from fastapi import APIRouter
       from scanner.api.health import router as health_router

       api_router = APIRouter()
       api_router.include_router(health_router)
       ```

    4. Create `src/scanner/main.py`:
       - `create_app()` factory function returning FastAPI instance
       - `lifespan` async context manager that:
         a. Creates ScannerSettings (using SCANNER_CONFIG_PATH env or default)
         b. Creates async engine via create_engine(settings.db_path)
         c. Creates tables with Base.metadata.create_all (Phase 1 approach; alembic for later)
         d. Stores engine, session_factory, settings on app.state
         e. On shutdown: await engine.dispose()
       - FastAPI app with title="aipix-security-scanner", version="0.1.0", lifespan=lifespan
       - Include api_router with prefix="/api"
       - Module-level `app = create_app()` for uvicorn

    5. Create `tests/test_health.py`:
       - Use httpx.AsyncClient with ASGITransport for testing
       - Override config to use tmp_path for DB (set SCANNER_DB_PATH and SCANNER_CONFIG_PATH env vars)
       - Create test app via create_app()
       - Tests from behavior list

    TDD flow: Write tests (RED), implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && pytest tests/test_health.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/api/health.py contains "class HealthResponse(BaseModel)"
    - src/scanner/api/health.py contains '@router.get("/health"'
    - src/scanner/api/health.py contains "SELECT 1"
    - src/scanner/api/health.py contains '"healthy"'
    - src/scanner/api/health.py contains '"degraded"'
    - src/scanner/api/router.py contains "api_router = APIRouter()"
    - src/scanner/api/router.py contains "include_router(health_router)"
    - src/scanner/main.py contains "def create_app"
    - src/scanner/main.py contains "lifespan"
    - src/scanner/main.py contains 'prefix="/api"'
    - src/scanner/main.py contains "Base.metadata.create_all"
    - src/scanner/main.py contains 'app = create_app()'
    - `pytest tests/test_health.py -x` exits 0
  </acceptance_criteria>
  <done>
    FastAPI app starts, creates SQLite DB tables on startup, and GET /api/health returns 200 with status=healthy, version, uptime, and database connectivity. All health endpoint tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/alex/naveksoft-security
pytest tests/test_models.py tests/test_health.py -x -v --tb=short
python -c "from scanner.main import app; print(f'App: {app.title} v{app.version}')"
```
</verification>

<success_criteria>
- All model tests pass (WAL mode, foreign keys, CRUD, persistence)
- Health endpoint returns 200 with all required fields
- Tables auto-created on startup
- Alembic skeleton exists for future migrations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-models/01-02-SUMMARY.md`
</output>
