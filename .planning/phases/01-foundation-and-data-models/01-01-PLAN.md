---
phase: 01-foundation-and-data-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/scanner/__init__.py
  - src/scanner/config.py
  - src/scanner/schemas/__init__.py
  - src/scanner/schemas/severity.py
  - src/scanner/schemas/finding.py
  - src/scanner/schemas/scan.py
  - src/scanner/core/__init__.py
  - src/scanner/core/fingerprint.py
  - config.yml.example
  - .env.example
  - .gitignore
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_fingerprint.py
autonomous: true
requirements:
  - SCAN-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Config loads from config.yml with env var overrides taking priority"
    - "Severity enum has 5 levels (Critical/High/Medium/Low/Info) with correct integer ordering"
    - "Fingerprint is deterministic: same inputs always produce same SHA-256 hash"
    - "No credentials or secrets exist in committed config files"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, pytest config"
      contains: "pydantic-settings"
    - path: "src/scanner/config.py"
      provides: "ScannerSettings with YAML + env var loading"
      contains: "YamlConfigSettingsSource"
    - path: "src/scanner/schemas/severity.py"
      provides: "Severity IntEnum"
      contains: "class Severity(IntEnum)"
    - path: "src/scanner/schemas/finding.py"
      provides: "Finding Pydantic schema"
      contains: "class FindingSchema"
    - path: "src/scanner/schemas/scan.py"
      provides: "ScanResult Pydantic schema"
      contains: "class ScanResultSchema"
    - path: "src/scanner/core/fingerprint.py"
      provides: "Deterministic dedup fingerprint"
      contains: "def compute_fingerprint"
    - path: "config.yml.example"
      provides: "Template config with placeholder values"
    - path: ".env.example"
      provides: "Template env vars with placeholder values"
  key_links:
    - from: "src/scanner/config.py"
      to: "config.yml"
      via: "YamlConfigSettingsSource reads yaml_file"
      pattern: "yaml_file.*config\\.yml"
    - from: "src/scanner/schemas/finding.py"
      to: "src/scanner/schemas/severity.py"
      via: "Finding uses Severity enum for severity field"
      pattern: "from.*severity.*import.*Severity"
    - from: "src/scanner/core/fingerprint.py"
      to: "hashlib"
      via: "SHA-256 hashing"
      pattern: "hashlib\\.sha256"
---

<objective>
Create the project skeleton with Python packaging, config system (YAML + env var overrides), Pydantic schemas for Finding and ScanResult, Severity enum, and deterministic fingerprint module.

Purpose: Establish the foundational types and config that every subsequent plan and phase depends on. This is the data contract for the entire scanner.
Output: Installable Python package with config, schemas, fingerprint logic, and passing unit tests.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-models/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project skeleton, config system, and Pydantic schemas</name>
  <files>
    pyproject.toml
    .gitignore
    config.yml.example
    .env.example
    src/scanner/__init__.py
    src/scanner/config.py
    src/scanner/schemas/__init__.py
    src/scanner/schemas/severity.py
    src/scanner/schemas/finding.py
    src/scanner/schemas/scan.py
    tests/__init__.py
    tests/conftest.py
    tests/test_config.py
  </files>
  <read_first>
    .planning/phases/01-foundation-and-data-models/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_severity_values: Severity.CRITICAL==5, HIGH==4, MEDIUM==3, LOW==2, INFO==1
    - test_severity_ordering: Severity.CRITICAL > Severity.HIGH > Severity.MEDIUM > Severity.LOW > Severity.INFO
    - test_severity_from_int: Severity(5) == Severity.CRITICAL
    - test_yaml_loading: ScannerSettings loads host, port, db_path from a temp config.yml file
    - test_env_override: Setting SCANNER_PORT=9999 overrides port=8000 from YAML
    - test_default_values: ScannerSettings() without YAML or env vars returns defaults (host="0.0.0.0", port=8000, db_path="/data/scanner.db")
    - test_no_hardcoded_secrets: ScannerSettings default api_key=="" and claude_api_key==""
    - test_config_path_env: SCANNER_CONFIG_PATH env var controls which YAML file is loaded
    - test_finding_schema: FindingSchema validates with all required fields (tool, rule_id, file_path, severity, title, fingerprint)
    - test_finding_schema_severity_validation: FindingSchema rejects severity values outside 1-5
    - test_scan_result_schema: ScanResultSchema validates with status, timestamps, counts
  </behavior>
  <action>
    1. Create `pyproject.toml` with:
       - name: "aipix-security-scanner"
       - version: "0.1.0"
       - requires-python: ">=3.12"
       - dependencies: ["fastapi[standard]", "pydantic-settings[yaml]", "sqlalchemy>=2.0", "aiosqlite", "alembic"]
       - optional dev dependencies: ["pytest", "pytest-asyncio", "httpx"]
       - [tool.pytest.ini_options]: asyncio_mode = "auto", testpaths = ["tests"]
       - [project.scripts] or [build-system] as appropriate

    2. Create `.gitignore` with Python defaults plus:
       - config.yml (actual config with secrets)
       - *.db, *.db-wal, *.db-shm (SQLite files)
       - .env (actual env file)
       - __pycache__/, *.pyc, .pytest_cache/
       - dist/, *.egg-info/

    3. Create `config.yml.example`:
       ```yaml
       # aipix-security-scanner configuration
       # Copy to config.yml and customize. Secrets should use env vars instead.
       host: "0.0.0.0"
       port: 8000
       db_path: "/data/scanner.db"
       api_key: ""  # Set via SCANNER_API_KEY env var
       claude_api_key: ""  # Set via SCANNER_CLAUDE_API_KEY env var
       slack_webhook_url: ""
       email_smtp_host: ""
       log_level: "info"
       scan_timeout: 600
       ```

    4. Create `.env.example`:
       ```
       # aipix-security-scanner environment variables
       # Copy to .env and fill in real values
       SCANNER_API_KEY=your-api-key-here
       SCANNER_CLAUDE_API_KEY=your-claude-api-key-here
       SCANNER_DB_PATH=/data/scanner.db
       SCANNER_PORT=8000
       SCANNER_CONFIG_PATH=config.yml
       ```

    5. Create `src/scanner/__init__.py` with `__version__ = "0.1.0"`

    6. Create `src/scanner/schemas/severity.py`:
       - `class Severity(IntEnum)` with CRITICAL=5, HIGH=4, MEDIUM=3, LOW=2, INFO=1
       - Exact code from RESEARCH.md Pattern 2

    7. Create `src/scanner/config.py`:
       - `class ScannerSettings(BaseSettings)` with fields:
         - host: str = "0.0.0.0"
         - port: int = 8000
         - db_path: str = "/data/scanner.db"
         - api_key: str = Field(default="", description="API key for authentication")
         - claude_api_key: str = Field(default="", description="Claude API key")
         - slack_webhook_url: str = ""
         - email_smtp_host: str = ""
         - log_level: str = "info"
         - scan_timeout: int = 600
       - model_config with:
         - yaml_file from env var SCANNER_CONFIG_PATH defaulting to "config.yml"
         - env_prefix="SCANNER_"
         - env_nested_delimiter="__"
       - settings_customise_sources: priority order init > env > dotenv > file_secret > yaml (env overrides yaml)
       - Use `import os` to read SCANNER_CONFIG_PATH: `yaml_file=os.environ.get("SCANNER_CONFIG_PATH", "config.yml")`

    8. Create `src/scanner/schemas/finding.py`:
       - `class FindingSchema(BaseModel)` with fields:
         - fingerprint: str (SHA-256 hex, 64 chars)
         - tool: str (semgrep, gitleaks, trivy, cppcheck, checkov)
         - rule_id: str
         - file_path: str
         - line_start: int | None = None
         - line_end: int | None = None
         - snippet: str | None = None
         - severity: Severity (import from severity.py)
         - title: str
         - description: str | None = None
         - recommendation: str | None = None
         - ai_analysis: str | None = None
         - ai_fix_suggestion: str | None = None
         - false_positive: bool = False
         - created_at: datetime | None = None

    9. Create `src/scanner/schemas/scan.py`:
       - `class ScanResultSchema(BaseModel)` with fields:
         - id: int | None = None
         - target_path: str | None = None
         - repo_url: str | None = None
         - branch: str | None = None
         - commit_hash: str | None = None
         - status: str = "pending" (literal: pending/running/completed/failed)
         - started_at: datetime | None = None
         - completed_at: datetime | None = None
         - duration_seconds: float | None = None
         - total_findings: int = 0
         - critical_count: int = 0
         - high_count: int = 0
         - medium_count: int = 0
         - low_count: int = 0
         - info_count: int = 0
         - gate_passed: bool | None = None
         - scanner_version: str | None = None
         - tool_versions: dict | None = None
         - error_message: str | None = None
         - created_at: datetime | None = None

    10. Create `src/scanner/schemas/__init__.py` exporting Severity, FindingSchema, ScanResultSchema

    11. Create `tests/__init__.py` (empty)

    12. Create `tests/conftest.py` with:
        - Fixture `tmp_config_file(tmp_path)` that writes a temporary config.yml
        - Fixture `clean_env(monkeypatch)` that clears SCANNER_* env vars
        - Import pytest

    13. Create `tests/test_config.py` with tests matching the behavior list above

    14. Install the project: `pip install -e ".[dev]"`

    TDD flow: Write tests first, run (RED), then implement, run (GREEN).
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && pip install -e ".[dev]" && pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains 'name = "aipix-security-scanner"'
    - pyproject.toml contains "pydantic-settings[yaml]"
    - pyproject.toml contains "sqlalchemy"
    - pyproject.toml contains "aiosqlite"
    - pyproject.toml contains 'asyncio_mode = "auto"'
    - src/scanner/__init__.py contains '__version__ = "0.1.0"'
    - src/scanner/schemas/severity.py contains "class Severity(IntEnum)"
    - src/scanner/schemas/severity.py contains "CRITICAL = 5"
    - src/scanner/schemas/severity.py contains "INFO = 1"
    - src/scanner/config.py contains "class ScannerSettings(BaseSettings)"
    - src/scanner/config.py contains "YamlConfigSettingsSource"
    - src/scanner/config.py contains 'env_prefix="SCANNER_"'
    - src/scanner/config.py contains "SCANNER_CONFIG_PATH"
    - src/scanner/schemas/finding.py contains "class FindingSchema(BaseModel)"
    - src/scanner/schemas/finding.py contains "severity: Severity"
    - src/scanner/schemas/scan.py contains "class ScanResultSchema(BaseModel)"
    - config.yml.example contains "db_path"
    - config.yml.example contains "api_key"
    - .env.example contains "SCANNER_API_KEY"
    - .env.example contains "SCANNER_CLAUDE_API_KEY"
    - .gitignore contains "config.yml"
    - .gitignore contains "*.db"
    - .gitignore contains ".env"
    - `pytest tests/test_config.py -x` exits 0
  </acceptance_criteria>
  <done>
    All config, schema, and severity tests pass. ScannerSettings loads from YAML with env var overrides. Severity enum has 5 levels with correct ordering. FindingSchema and ScanResultSchema validate correctly. No secrets in committed files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fingerprint module with deterministic dedup hashing</name>
  <files>
    src/scanner/core/__init__.py
    src/scanner/core/fingerprint.py
    tests/test_fingerprint.py
  </files>
  <read_first>
    .planning/phases/01-foundation-and-data-models/01-RESEARCH.md
    src/scanner/schemas/severity.py
  </read_first>
  <behavior>
    - test_basic_fingerprint: compute_fingerprint("src/auth.py", "sql-injection", "query = f'SELECT * FROM users WHERE id={user_id}'") returns a 64-char hex string
    - test_deterministic: same inputs always produce identical output (call twice, assert equal)
    - test_path_normalization: ".\src\auth.py" and "src/auth.py" produce same fingerprint
    - test_whitespace_normalization: "x  =  1" and "x = 1" produce same fingerprint
    - test_rule_case_normalization: "SQL-Injection" and "sql-injection" produce same fingerprint
    - test_different_rules_differ: same file+snippet but different rule_id produces different fingerprint
    - test_different_files_differ: same rule+snippet but different file_path produces different fingerprint
    - test_different_snippets_differ: same file+rule but different snippet produces different fingerprint
    - test_empty_snippet: compute_fingerprint("file.py", "rule-1", "") produces valid 64-char hex
    - test_unicode_snippet: compute_fingerprint with Unicode characters produces valid fingerprint
  </behavior>
  <action>
    1. Create `src/scanner/core/__init__.py` (empty)

    2. Create `src/scanner/core/fingerprint.py` with:
       ```python
       import hashlib
       import re

       def compute_fingerprint(file_path: str, rule_id: str, snippet: str) -> str:
           """Deterministic fingerprint for finding deduplication.
           Normalizes inputs before hashing:
           - file_path: forward slashes, no leading ./, stripped
           - rule_id: lowercase, stripped
           - snippet: whitespace-collapsed, stripped
           """
           norm_path = file_path.replace("\\", "/").lstrip("./").strip()
           norm_rule = rule_id.lower().strip()
           norm_snippet = re.sub(r"\s+", " ", snippet).strip()
           content = f"{norm_path}:{norm_rule}:{norm_snippet}"
           return hashlib.sha256(content.encode("utf-8")).hexdigest()
       ```

    3. Create `tests/test_fingerprint.py` with all tests from the behavior list.

    TDD flow: Write tests first (RED), implement (GREEN), verify all pass.
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && pytest tests/test_fingerprint.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/core/fingerprint.py contains "def compute_fingerprint("
    - src/scanner/core/fingerprint.py contains "hashlib.sha256"
    - src/scanner/core/fingerprint.py contains 're.sub(r"\\s+"'
    - src/scanner/core/fingerprint.py contains '.replace("\\\\", "/")'
    - src/scanner/core/fingerprint.py contains ".lower().strip()"
    - tests/test_fingerprint.py contains "test_deterministic"
    - tests/test_fingerprint.py contains "test_path_normalization"
    - tests/test_fingerprint.py contains "test_whitespace_normalization"
    - `pytest tests/test_fingerprint.py -x` exits 0
  </acceptance_criteria>
  <done>
    Fingerprint module produces deterministic SHA-256 hashes. Path separators, whitespace, and rule ID casing are normalized. All 10 test cases pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/alex/naveksoft-security
pytest tests/ -x -v --tb=short
python -c "from scanner.config import ScannerSettings; print('Config OK')"
python -c "from scanner.schemas.severity import Severity; assert Severity.CRITICAL > Severity.HIGH; print('Severity OK')"
python -c "from scanner.core.fingerprint import compute_fingerprint; assert len(compute_fingerprint('f','r','s'))==64; print('Fingerprint OK')"
```
</verification>

<success_criteria>
- All tests in tests/test_config.py and tests/test_fingerprint.py pass
- ScannerSettings loads config with env var overrides
- Severity enum has 5 ordered levels
- FindingSchema and ScanResultSchema validate correctly
- Fingerprint is deterministic with normalization
- No secrets in config.yml.example or .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-models/01-01-SUMMARY.md`
</output>
