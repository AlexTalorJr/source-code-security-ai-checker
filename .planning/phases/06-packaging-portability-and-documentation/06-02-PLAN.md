---
phase: 06-packaging-portability-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/en/architecture.md
  - docs/en/database-schema.md
  - docs/en/user-guide.md
  - docs/en/admin-guide.md
  - docs/en/custom-rules.md
  - tests/phase_06/test_docs.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-09

must_haves:
  truths:
    - "README.md is English-only with quick start guide (5 min to first scan)"
    - "docs/en/ directory contains architecture.md updated for Phases 1-5"
    - "docs/en/database-schema.md contains Mermaid ER diagram with all models"
    - "docs/en/user-guide.md covers reports, AI findings, quality gate"
    - "docs/en/admin-guide.md covers all configuration from Phases 1-5"
    - "docs/en/custom-rules.md covers writing Semgrep rules"
  artifacts:
    - path: "README.md"
      provides: "English quick start guide"
      contains: "## Quick Start"
    - path: "docs/en/architecture.md"
      provides: "System architecture documentation"
      contains: "## Component"
    - path: "docs/en/database-schema.md"
      provides: "Database schema with ER diagram"
      contains: "erDiagram"
    - path: "docs/en/user-guide.md"
      provides: "User guide for reports and findings"
      contains: "## Reports"
    - path: "docs/en/admin-guide.md"
      provides: "Admin configuration guide"
      contains: "## Configuration"
    - path: "docs/en/custom-rules.md"
      provides: "Custom Semgrep rules guide"
      contains: "Semgrep"
    - path: "tests/phase_06/test_docs.py"
      provides: "Documentation structure validation"
  key_links:
    - from: "README.md"
      to: "docs/en/"
      via: "documentation links"
      pattern: "docs/en/"
---

<objective>
Restructure documentation: move existing 8 docs from `docs/` to `docs/en/`, rewrite README.md as English-only, update first 5 English docs to cover Phases 1-5 features, and create doc test scaffolds.

Purpose: English documentation must be complete and current before Russian translation (Plan 04).
Output: Restructured docs/en/ with 5 updated docs, English-only README, doc tests.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-packaging-portability-and-documentation/06-CONTEXT.md
@.planning/phases/06-packaging-portability-and-documentation/06-RESEARCH.md

<interfaces>
From config.yml.example (all configurable options docs must cover):
```yaml
host, port, db_path, api_key, claude_api_key, slack_webhook_url, email_smtp_host, log_level, scan_timeout
scanners: semgrep, cppcheck, gitleaks, trivy, checkov (each with enabled, timeout, extra_args)
ai: max_cost_per_scan, model, max_findings_per_batch, max_tokens_per_response
gate: fail_on (critical, high), include_compound_risks
notifications: slack (enabled), email (enabled, recipients, smtp_port, smtp_user, smtp_password, use_tls)
dashboard_url
```

From .env.example:
```
SCANNER_API_KEY, SCANNER_CLAUDE_API_KEY, SCANNER_DB_PATH, SCANNER_PORT, SCANNER_CONFIG_PATH
```

Existing docs to move (all in docs/):
architecture.md (96 lines), database-schema.md (103 lines), user-guide.md (77 lines),
admin-guide.md (121 lines), api.md (98 lines), devops-guide.md (162 lines),
transfer-guide.md (141 lines), custom-rules.md (77 lines)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doc tests, move docs to docs/en/, rewrite README.md</name>
  <files>tests/phase_06/test_docs.py, README.md, docs/en/architecture.md, docs/en/database-schema.md, docs/en/user-guide.md, docs/en/admin-guide.md, docs/en/custom-rules.md</files>
  <read_first>
    - README.md
    - docs/architecture.md
    - docs/database-schema.md
    - docs/user-guide.md
    - docs/admin-guide.md
    - docs/custom-rules.md
    - config.yml.example
    - .env.example
    - tests/phase_06/__init__.py
  </read_first>
  <action>
**Step 1: Create tests/phase_06/test_docs.py** with these tests:

```python
# Use pathlib.Path to find project root (traverse up from __file__ to find pyproject.toml)
```

- `test_readme_exists()` -- README.md exists
- `test_readme_is_english_only()` -- README.md does NOT contain Russian text markers ("Быстрый старт", "Описание", "Документация"). It should be pure English.
- `test_readme_has_quick_start()` -- README.md contains "## Quick Start"
- `test_readme_links_to_docs_en()` -- README.md contains "docs/en/" (links point to new location)
- `test_readme_ru_exists()` -- README.ru.md exists (will be created in Plan 04, so mark xfail for now with `@pytest.mark.xfail(reason="Created in Plan 04")`)
- `test_docs_en_directory_exists()` -- docs/en/ directory exists
- `test_docs_en_has_all_files()` -- for each of ["architecture.md", "database-schema.md", "user-guide.md", "admin-guide.md", "devops-guide.md", "api.md", "transfer-guide.md", "custom-rules.md"], assert file exists in docs/en/
- `test_docs_en_are_english_only()` -- for each doc in docs/en/, assert it does NOT contain Russian section markers ("/ Описание", "/ Архитектура", "/ Обзор")
- `test_docs_structure()` -- each doc in docs/en/ has "# " title line
- `test_russian_docs()` -- docs/ru/ directory exists AND all 8 matching files exist (mark xfail, created in Plan 04)
- `test_meta_files_license()` -- LICENSE file exists and contains "Apache License" (mark xfail, created in Plan 03)
- `test_meta_files_contributing()` -- CONTRIBUTING.md exists (mark xfail, created in Plan 03)
- `test_meta_files_changelog()` -- CHANGELOG.md exists and contains "Phase 5" (mark xfail for Phase 5 content, created in Plan 03)
- `test_meta_files_env_example()` -- .env.example exists

**Step 2: Move docs to docs/en/**
- `mkdir -p docs/en`
- For each of the 8 docs: `git mv docs/{file} docs/en/{file}`
- Remove old docs/ files (the git mv handles this)

**Step 3: Rewrite README.md** as English-only:
- Title: `# aipix-security-scanner`
- Tagline: "AI-powered security vulnerability scanner for the aipix.ai VSaaS platform."
- `## Quick Start` section with 5-minute path: prerequisites (Docker + Docker Compose), clone, configure (cp config.yml.example config.yml, cp .env.example .env), set secrets (SCANNER_API_KEY, SCANNER_CLAUDE_API_KEY), launch (docker compose up -d), verify (curl health endpoint). Show expected JSON response.
- `## Features` section listing: 5 parallel security scanners, AI-powered analysis with Claude, interactive HTML and PDF reports, configurable quality gate, REST API with web dashboard, Slack and email notifications, Jenkins CI integration, scan history with delta comparison
- `## Documentation` table linking to docs/en/ paths
- `## Project Status` table updated with all 6 phases and their actual statuses
- `## Tech Stack` section listing Python 3.12, FastAPI, SQLAlchemy 2.0, SQLite WAL, Pydantic v2, Docker, Alembic, Anthropic Claude, WeasyPrint, Jinja2, Typer
- `## License` -- Apache 2.0
- Note at bottom: "Russian documentation: [README.ru.md](README.ru.md)"

**Step 4: Strip Russian from English docs and update content**

For each of the 5 docs (architecture.md, database-schema.md, user-guide.md, admin-guide.md, custom-rules.md):
1. Read current content
2. Remove all Russian text (lines after "/" separators that contain Cyrillic, standalone Russian paragraphs)
3. Update content to cover Phases 1-5 features

Specific updates per doc:

**docs/en/architecture.md** (~150-200 lines):
- Update component diagram Mermaid to show all layers: API Server (FastAPI), Scanner Orchestrator (parallel execution), 5 Adapters (Semgrep, cppcheck, Gitleaks, Trivy, Checkov), AI Analyzer (Claude), Report Generator (HTML/PDF), Quality Gate, Dashboard, Notification Dispatcher (Slack/Email), Scan Queue, Database (SQLite)
- Add sections: Data Flow (scan lifecycle from API trigger to notification), Technology Choices (with rationale), Security Model (API key auth, non-root Docker)
- Update configuration section to reference all SCANNER_ env vars

**docs/en/database-schema.md** (~120-150 lines):
- Update Mermaid ER diagram to include ALL models: ScanResult, Finding, CompoundRisk, Suppression
- Add fields for each model based on actual ORM: ScanResult (id, repo_url, branch, commit_hash, status, started_at, completed_at, findings_count, critical_count, high_count, medium_count, low_count, info_count, gate_passed, ai_analysis_cost), Finding (id, scan_result_id, tool, severity, rule_id, file_path, line_start, line_end, snippet, message, fingerprint, ai_fix_suggestion, is_suppressed), CompoundRisk (id, scan_result_id, title, severity, description, finding_ids, risk_category), Suppression (id, fingerprint, reason, created_at)
- Document indexes and relationships
- Note WAL mode configuration

**docs/en/user-guide.md** (~120-150 lines):
- Add sections for: Running a Scan (CLI and API), Understanding Severity Levels, Reading HTML Reports (filters, code context, AI suggestions), Reading PDF Reports (executive summary, charts), Quality Gate results, AI Analysis (compound risks, fix suggestions), Delta Comparison (new/fixed/persisting), Managing False Positives (suppression via API/dashboard)

**docs/en/admin-guide.md** (~150-180 lines):
- Document ALL config.yml sections: scanners (per-tool enable/disable, timeout, extra_args), AI (model, cost limit, batch size, token limit), quality gate (fail_on severities, include_compound_risks), notifications (Slack webhook, email SMTP settings), dashboard_url
- Document ALL environment variables: SCANNER_API_KEY, SCANNER_CLAUDE_API_KEY, SCANNER_DB_PATH, SCANNER_PORT, SCANNER_CONFIG_PATH, plus nested notification vars (SCANNER_NOTIFICATIONS__EMAIL__SMTP_PASSWORD etc.)
- Add sections: Threshold Tuning, Scanner Tool Management, Performance Tuning (timeouts, batch sizes)

**docs/en/custom-rules.md** (~80-100 lines):
- Remove Russian, keep English content mostly as-is
- Ensure it covers: Semgrep rule format, aipix-specific patterns (auth bypass, IDOR, SQL injection), rule file location, testing rules
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && python -m pytest tests/phase_06/test_docs.py -x -v -k "not russian_docs and not meta_files_license and not meta_files_contributing and not meta_files_changelog and not readme_ru"</automated>
  </verify>
  <acceptance_criteria>
    - docs/en/ directory exists
    - docs/en/architecture.md exists and does NOT contain "/ Архитектура"
    - docs/en/database-schema.md exists and contains "erDiagram"
    - docs/en/user-guide.md exists and contains "## Reports" or "## Running a Scan"
    - docs/en/admin-guide.md exists and contains "## Configuration" or "## Environment Variables"
    - docs/en/custom-rules.md exists and contains "Semgrep"
    - README.md contains "## Quick Start" and does NOT contain "Быстрый старт"
    - README.md contains "docs/en/" in documentation links
    - docs/ root no longer contains the 8 .md files (moved to docs/en/)
    - tests/phase_06/test_docs.py contains "def test_readme_has_quick_start"
    - tests/phase_06/test_docs.py contains "def test_docs_en_has_all_files"
    - Non-xfail tests in test_docs.py pass
  </acceptance_criteria>
  <done>Documentation restructured: 8 docs moved to docs/en/ with Russian stripped, 5 docs updated for Phases 1-5, README.md rewritten as English-only with quick start guide, doc test scaffolds created</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/phase_06/test_docs.py -x -v -k "not russian_docs and not meta_files_license and not meta_files_contributing and not meta_files_changelog and not readme_ru"` -- non-xfail tests pass
- `ls docs/en/` shows 8 markdown files
- `ls docs/*.md 2>/dev/null` shows no files (all moved)
- README.md links point to docs/en/ paths
</verification>

<success_criteria>
All 8 docs live in docs/en/ with Russian removed. 5 docs (architecture, database-schema, user-guide, admin-guide, custom-rules) updated to reflect Phases 1-5. README.md is English-only with quick start. Doc tests validate structure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging-portability-and-documentation/06-02-SUMMARY.md`
</output>
