---
phase: 09-tier-1-scanner-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase_09/__init__.py
  - tests/phase_09/conftest.py
  - tests/phase_09/fixtures/gosec_output.json
  - tests/phase_09/fixtures/bandit_output.json
  - tests/phase_09/test_adapter_gosec.py
  - tests/phase_09/test_adapter_bandit.py
  - src/scanner/adapters/gosec.py
  - src/scanner/adapters/bandit.py
autonomous: true
requirements: [SCAN-01, SCAN-02]

must_haves:
  truths:
    - "gosec adapter parses native JSON output into FindingSchema list"
    - "gosec severity mapping: HIGH->HIGH, MEDIUM->MEDIUM, LOW->LOW"
    - "Bandit adapter parses native JSON with confidence x severity matrix"
    - "Bandit HIGH sev + HIGH conf -> CRITICAL, HIGH sev + MED conf -> HIGH"
    - "Both adapters treat exit code 1 as findings-found, not error"
    - "Both adapters raise ScannerExecutionError on exit code >= 2"
  artifacts:
    - path: "src/scanner/adapters/gosec.py"
      provides: "GosecAdapter subclass of ScannerAdapter"
      exports: ["GosecAdapter"]
    - path: "src/scanner/adapters/bandit.py"
      provides: "BanditAdapter subclass of ScannerAdapter"
      exports: ["BanditAdapter"]
    - path: "tests/phase_09/test_adapter_gosec.py"
      provides: "Unit tests for gosec adapter"
      min_lines: 50
    - path: "tests/phase_09/test_adapter_bandit.py"
      provides: "Unit tests for Bandit adapter"
      min_lines: 50
  key_links:
    - from: "src/scanner/adapters/gosec.py"
      to: "scanner.adapters.base.ScannerAdapter"
      via: "class inheritance"
      pattern: "class GosecAdapter\\(ScannerAdapter\\)"
    - from: "src/scanner/adapters/bandit.py"
      to: "scanner.adapters.base.ScannerAdapter"
      via: "class inheritance"
      pattern: "class BanditAdapter\\(ScannerAdapter\\)"
---

<objective>
Implement gosec (Go SAST) and Bandit (Python SAST) scanner adapters with full test coverage.

Purpose: Enable scanning Go and Python projects with dedicated security tools (SCAN-01, SCAN-02).
Output: Two adapter modules, test infrastructure (conftest + fixtures), and passing tests.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tier-1-scanner-adapters/09-CONTEXT.md
@.planning/phases/09-tier-1-scanner-adapters/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/scanner/adapters/base.py:
```python
class ScannerAdapter(abc.ABC):
    @property
    @abc.abstractmethod
    def tool_name(self) -> str: ...

    @abc.abstractmethod
    async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]: ...

    @abc.abstractmethod
    def _version_command(self) -> list[str]: ...

    async def _execute(self, cmd: list[str], timeout: int) -> tuple[str, str, int]: ...

    @staticmethod
    def _normalize_path(file_path: str, target_prefix: str) -> str: ...
```

From src/scanner/schemas/finding.py:
```python
class FindingSchema(BaseModel):
    fingerprint: str       # SHA-256 hex, 64 chars
    tool: str
    rule_id: str
    file_path: str
    line_start: int | None = None
    line_end: int | None = None
    snippet: str | None = None
    severity: Severity
    title: str
    description: str | None = None
```

From src/scanner/schemas/severity.py:
```python
class Severity(IntEnum):
    CRITICAL = 5
    HIGH = 4
    MEDIUM = 3
    LOW = 2
    INFO = 1
```

From src/scanner/core/fingerprint.py:
```python
def compute_fingerprint(file_path: str, rule_id: str, snippet: str) -> str: ...
```

From src/scanner/core/exceptions.py:
```python
class ScannerExecutionError(ScannerError):
    def __init__(self, tool_name: str, message: str, returncode: int | None = None): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and gosec adapter with tests</name>
  <files>
    tests/phase_09/__init__.py
    tests/phase_09/conftest.py
    tests/phase_09/fixtures/gosec_output.json
    tests/phase_09/test_adapter_gosec.py
    src/scanner/adapters/gosec.py
  </files>
  <read_first>
    src/scanner/adapters/semgrep.py
    src/scanner/adapters/base.py
    src/scanner/schemas/finding.py
    src/scanner/schemas/severity.py
    src/scanner/core/fingerprint.py
    src/scanner/core/exceptions.py
    tests/phase_02/conftest.py
    tests/phase_02/test_adapter_semgrep.py
    tests/phase_02/fixtures/semgrep_output.json
  </read_first>
  <action>
**Step 1: Create test infrastructure.**

Create `tests/phase_09/__init__.py` (empty file).

Create `tests/phase_09/conftest.py` following the phase_02 pattern:
```python
"""Shared fixtures for Phase 09 scanner adapter tests."""
import json
from pathlib import Path
import pytest

@pytest.fixture
def fixtures_dir() -> Path:
    return Path(__file__).parent / "fixtures"

@pytest.fixture
def gosec_output(fixtures_dir: Path) -> dict:
    return json.loads((fixtures_dir / "gosec_output.json").read_text())

@pytest.fixture
def bandit_output(fixtures_dir: Path) -> dict:
    return json.loads((fixtures_dir / "bandit_output.json").read_text())

@pytest.fixture
def brakeman_output(fixtures_dir: Path) -> dict:
    return json.loads((fixtures_dir / "brakeman_output.json").read_text())

@pytest.fixture
def cargo_audit_output(fixtures_dir: Path) -> dict:
    return json.loads((fixtures_dir / "cargo_audit_output.json").read_text())
```

**Step 2: Create gosec fixture.**

Create `tests/phase_09/fixtures/gosec_output.json` with 3 findings of varying severity:
```json
{
  "Issues": [
    {
      "severity": "HIGH",
      "confidence": "LOW",
      "cwe": {"id": "798", "url": "https://cwe.mitre.org/data/definitions/798.html"},
      "rule_id": "G101",
      "details": "Potential hardcoded credentials",
      "file": "/tmp/target/cmd/main.go",
      "code": "var password = \"secret\"",
      "line": "32",
      "column": "6"
    },
    {
      "severity": "MEDIUM",
      "confidence": "HIGH",
      "cwe": {"id": "22", "url": "https://cwe.mitre.org/data/definitions/22.html"},
      "rule_id": "G304",
      "details": "File path provided as taint input",
      "file": "/tmp/target/pkg/handler.go",
      "code": "f, err := os.Open(userInput)",
      "line": "15",
      "column": "12"
    },
    {
      "severity": "LOW",
      "confidence": "HIGH",
      "cwe": {"id": "703", "url": "https://cwe.mitre.org/data/definitions/703.html"},
      "rule_id": "G104",
      "details": "Errors unhandled",
      "file": "/tmp/target/pkg/utils.go",
      "code": "_ = f.Close()",
      "line": "42",
      "column": "2"
    }
  ],
  "Stats": {"files": 10, "lines": 500, "nosec": 0, "found": 3}
}
```

**Step 3: Create gosec test file.**

Create `tests/phase_09/test_adapter_gosec.py` with these tests (follow semgrep test pattern):
1. `test_parse_gosec_findings` -- mock `_execute` with fixture JSON (exit code 1 = findings found), assert `len(findings) == 3`, assert all `f.tool == "gosec"`
2. `test_gosec_severity_mapping` -- assert G101 -> HIGH, G304 -> MEDIUM, G104 -> LOW
3. `test_gosec_path_normalization` -- assert no file_path starts with "/tmp/target"
4. `test_gosec_exit_code_1_is_not_error` -- exit code 1 returns findings normally
5. `test_gosec_exit_code_2_raises_error` -- exit code 2 raises ScannerExecutionError
6. `test_gosec_fingerprint_populated` -- each finding has 64-char hex fingerprint
7. `test_gosec_empty_results` -- `{"Issues": [], "Stats": {}}` returns empty list
8. `test_gosec_string_line_numbers` -- verify line_start is int (not string), line_end == line_start

Each test creates `GosecAdapter()` via a `@pytest.fixture def adapter(): return GosecAdapter()`. Use `AsyncMock` on `adapter._execute`.

**Step 4: Create gosec adapter.**

Create `src/scanner/adapters/gosec.py`:

```python
"""Gosec scanner adapter -- parses JSON output into FindingSchema."""

import json

from scanner.adapters.base import ScannerAdapter
from scanner.core.exceptions import ScannerExecutionError
from scanner.core.fingerprint import compute_fingerprint
from scanner.schemas.finding import FindingSchema
from scanner.schemas.severity import Severity

GOSEC_SEVERITY_MAP: dict[str, Severity] = {
    "HIGH": Severity.HIGH,
    "MEDIUM": Severity.MEDIUM,
    "LOW": Severity.LOW,
}


class GosecAdapter(ScannerAdapter):
    """Adapter for gosec Go security analysis tool."""

    @property
    def tool_name(self) -> str:
        return "gosec"

    def _version_command(self) -> list[str]:
        return ["gosec", "--version"]

    async def run(
        self,
        target_path: str,
        timeout: int,
        extra_args: list[str] | None = None,
    ) -> list[FindingSchema]:
        cmd = ["gosec", "-fmt=json", "-stdout", f"{target_path}/..."]
        if extra_args:
            cmd.extend(extra_args)

        stdout, stderr, returncode = await self._execute(cmd, timeout)

        # Exit code 1 = findings found (not error). Only >= 2 is error.
        if returncode >= 2:
            raise ScannerExecutionError(
                self.tool_name, stderr or "unknown error", returncode
            )

        if not stdout.strip():
            return []

        data = json.loads(stdout)
        issues = data.get("Issues", [])

        findings: list[FindingSchema] = []
        for issue in issues:
            rule_id = issue.get("rule_id", "unknown")
            raw_path = issue.get("file", "")
            rel_path = self._normalize_path(raw_path, target_path)
            snippet = issue.get("code", "")
            severity_str = issue.get("severity", "MEDIUM")
            severity = GOSEC_SEVERITY_MAP.get(severity_str, Severity.MEDIUM)
            # gosec line is a string, convert to int
            line_num = int(issue.get("line", "0")) or None

            fingerprint = compute_fingerprint(rel_path, rule_id, snippet)

            findings.append(
                FindingSchema(
                    fingerprint=fingerprint,
                    tool=self.tool_name,
                    rule_id=rule_id,
                    file_path=rel_path,
                    line_start=line_num,
                    line_end=line_num,  # single-line per CONTEXT.md
                    snippet=snippet,
                    severity=severity,
                    title=issue.get("details", rule_id),
                    description=issue.get("details"),
                )
            )

        return findings
```

Run `python -m pytest tests/phase_09/test_adapter_gosec.py -x -q` and verify all 8 tests pass.
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && python -m pytest tests/phase_09/test_adapter_gosec.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/adapters/gosec.py contains `class GosecAdapter(ScannerAdapter)`
    - src/scanner/adapters/gosec.py contains `GOSEC_SEVERITY_MAP`
    - src/scanner/adapters/gosec.py contains `def tool_name` returning `"gosec"`
    - src/scanner/adapters/gosec.py contains `int(issue.get("line"` (string-to-int conversion)
    - src/scanner/adapters/gosec.py contains `line_end=line_num` (single-line)
    - tests/phase_09/test_adapter_gosec.py contains at least 7 `async def test_` functions
    - tests/phase_09/fixtures/gosec_output.json contains `"Issues"` key with 3 entries
    - tests/phase_09/conftest.py contains `gosec_output` and `bandit_output` fixtures
    - `python -m pytest tests/phase_09/test_adapter_gosec.py -x -q` exits 0
  </acceptance_criteria>
  <done>GosecAdapter passes all unit tests: parses 3 findings, maps severity correctly, normalizes paths, handles exit codes 0/1/2+, produces 64-char fingerprints, converts string line numbers to int.</done>
</task>

<task type="auto">
  <name>Task 2: Create Bandit adapter with tests</name>
  <files>
    tests/phase_09/fixtures/bandit_output.json
    tests/phase_09/test_adapter_bandit.py
    src/scanner/adapters/bandit.py
  </files>
  <read_first>
    src/scanner/adapters/semgrep.py
    src/scanner/adapters/gosec.py
    src/scanner/adapters/base.py
    src/scanner/schemas/finding.py
    src/scanner/core/fingerprint.py
    src/scanner/core/exceptions.py
    tests/phase_09/test_adapter_gosec.py
  </read_first>
  <action>
**Step 1: Create Bandit fixture.**

Create `tests/phase_09/fixtures/bandit_output.json` with 4 findings covering the confidence x severity matrix:
```json
{
  "results": [
    {
      "code": "exec(user_input)\n",
      "col_offset": 0,
      "end_col_offset": 20,
      "filename": "/tmp/target/app.py",
      "issue_confidence": "HIGH",
      "issue_severity": "HIGH",
      "issue_cwe": {"id": 78, "link": "https://cwe.mitre.org/data/definitions/78.html"},
      "issue_text": "Use of exec detected.",
      "line_number": 10,
      "line_range": [10],
      "more_info": "https://bandit.readthedocs.io/en/latest/plugins/b102_exec_used.html",
      "test_id": "B102",
      "test_name": "exec_used"
    },
    {
      "code": "password = 'hardcoded'\n",
      "col_offset": 0,
      "end_col_offset": 25,
      "filename": "/tmp/target/config.py",
      "issue_confidence": "MEDIUM",
      "issue_severity": "HIGH",
      "issue_cwe": {"id": 259, "link": "https://cwe.mitre.org/data/definitions/259.html"},
      "issue_text": "Possible hardcoded password.",
      "line_number": 5,
      "line_range": [5],
      "more_info": "https://bandit.readthedocs.io/en/latest/plugins/b105_hardcoded_password_string.html",
      "test_id": "B105",
      "test_name": "hardcoded_password_string"
    },
    {
      "code": "hashlib.md5(data)\n",
      "col_offset": 0,
      "end_col_offset": 18,
      "filename": "/tmp/target/utils.py",
      "issue_confidence": "HIGH",
      "issue_severity": "MEDIUM",
      "issue_cwe": {"id": 327, "link": "https://cwe.mitre.org/data/definitions/327.html"},
      "issue_text": "Use of insecure MD5 hash function.",
      "line_number": 22,
      "line_range": [22],
      "more_info": "https://bandit.readthedocs.io/en/latest/plugins/b303_md5.html",
      "test_id": "B303",
      "test_name": "md5"
    },
    {
      "code": "os.chmod(path, 0o777)\n",
      "col_offset": 0,
      "end_col_offset": 22,
      "filename": "/tmp/target/setup.py",
      "issue_confidence": "LOW",
      "issue_severity": "LOW",
      "issue_cwe": {"id": 732, "link": "https://cwe.mitre.org/data/definitions/732.html"},
      "issue_text": "Insecure file permissions.",
      "line_number": 8,
      "line_range": [8],
      "more_info": "https://bandit.readthedocs.io/en/latest/plugins/b103_set_bad_file_permissions.html",
      "test_id": "B103",
      "test_name": "set_bad_file_permissions"
    }
  ],
  "errors": [],
  "generated_at": "2026-03-21T12:00:00Z",
  "metrics": {}
}
```

**Step 2: Create Bandit test file.**

Create `tests/phase_09/test_adapter_bandit.py` with these tests:
1. `test_parse_bandit_findings` -- mock `_execute` with fixture JSON (exit code 1), assert `len(findings) == 4`, assert all `f.tool == "bandit"`
2. `test_bandit_severity_matrix` -- assert B102 (HIGH/HIGH) -> CRITICAL, B105 (HIGH/MEDIUM) -> HIGH, B303 (MEDIUM/HIGH) -> MEDIUM, B103 (LOW/LOW) -> INFO
3. `test_bandit_path_normalization` -- assert no file_path starts with "/tmp/target"
4. `test_bandit_exit_code_1_is_not_error` -- exit code 1 returns findings normally
5. `test_bandit_exit_code_2_raises_error` -- exit code 2 raises ScannerExecutionError
6. `test_bandit_fingerprint_populated` -- each finding has 64-char hex fingerprint
7. `test_bandit_empty_results` -- `{"results": [], "errors": []}` returns empty list
8. `test_bandit_line_range` -- verify line_start from `line_number`, line_end from last element of `line_range`

Each test creates `BanditAdapter()` via a `@pytest.fixture def adapter(): return BanditAdapter()`.

**Step 3: Create Bandit adapter.**

Create `src/scanner/adapters/bandit.py`:

```python
"""Bandit scanner adapter -- parses JSON output into FindingSchema."""

import json

from scanner.adapters.base import ScannerAdapter
from scanner.core.exceptions import ScannerExecutionError
from scanner.core.fingerprint import compute_fingerprint
from scanner.schemas.finding import FindingSchema
from scanner.schemas.severity import Severity

# Confidence x severity matrix per CONTEXT.md decision
BANDIT_SEVERITY_MATRIX: dict[tuple[str, str], Severity] = {
    ("HIGH", "HIGH"): Severity.CRITICAL,
    ("HIGH", "MEDIUM"): Severity.HIGH,
    ("HIGH", "LOW"): Severity.MEDIUM,
    ("MEDIUM", "HIGH"): Severity.MEDIUM,
    ("MEDIUM", "MEDIUM"): Severity.MEDIUM,
    ("MEDIUM", "LOW"): Severity.LOW,
    ("LOW", "HIGH"): Severity.LOW,
    ("LOW", "MEDIUM"): Severity.LOW,
    ("LOW", "LOW"): Severity.INFO,
}


def _bandit_severity(issue_severity: str, issue_confidence: str) -> Severity:
    """Map Bandit severity using confidence x severity matrix."""
    key = (issue_severity.upper(), issue_confidence.upper())
    return BANDIT_SEVERITY_MATRIX.get(key, Severity.INFO)


class BanditAdapter(ScannerAdapter):
    """Adapter for Bandit Python security analysis tool."""

    @property
    def tool_name(self) -> str:
        return "bandit"

    def _version_command(self) -> list[str]:
        return ["bandit", "--version"]

    async def run(
        self,
        target_path: str,
        timeout: int,
        extra_args: list[str] | None = None,
    ) -> list[FindingSchema]:
        cmd = [
            "bandit", "-r", target_path, "-f", "json",
        ]
        if extra_args:
            cmd.extend(extra_args)

        stdout, stderr, returncode = await self._execute(cmd, timeout)

        # Exit code 1 = findings found (not error). Only >= 2 is error.
        if returncode >= 2:
            raise ScannerExecutionError(
                self.tool_name, stderr or "unknown error", returncode
            )

        if not stdout.strip():
            return []

        data = json.loads(stdout)
        results = data.get("results", [])

        findings: list[FindingSchema] = []
        for result in results:
            test_id = result.get("test_id", "unknown")
            raw_path = result.get("filename", "")
            rel_path = self._normalize_path(raw_path, target_path)
            snippet = result.get("code", "")
            severity = _bandit_severity(
                result.get("issue_severity", "LOW"),
                result.get("issue_confidence", "LOW"),
            )
            line_start = result.get("line_number")
            line_range = result.get("line_range", [])
            line_end = line_range[-1] if line_range else line_start

            fingerprint = compute_fingerprint(rel_path, test_id, snippet)

            findings.append(
                FindingSchema(
                    fingerprint=fingerprint,
                    tool=self.tool_name,
                    rule_id=test_id,
                    file_path=rel_path,
                    line_start=line_start,
                    line_end=line_end,
                    snippet=snippet,
                    severity=severity,
                    title=result.get("issue_text", test_id),
                    description=result.get("issue_text"),
                )
            )

        return findings
```

Run `python -m pytest tests/phase_09/test_adapter_bandit.py -x -q` and verify all 8 tests pass.
Then run `python -m pytest tests/phase_09/ -x -q` to confirm all Phase 09 tests pass together.
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && python -m pytest tests/phase_09/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/adapters/bandit.py contains `class BanditAdapter(ScannerAdapter)`
    - src/scanner/adapters/bandit.py contains `BANDIT_SEVERITY_MATRIX`
    - src/scanner/adapters/bandit.py contains `def _bandit_severity(issue_severity: str, issue_confidence: str)`
    - src/scanner/adapters/bandit.py contains `("HIGH", "HIGH"): Severity.CRITICAL`
    - src/scanner/adapters/bandit.py contains `("HIGH", "MEDIUM"): Severity.HIGH`
    - src/scanner/adapters/bandit.py contains `def tool_name` returning `"bandit"`
    - tests/phase_09/test_adapter_bandit.py contains at least 7 `async def test_` functions
    - tests/phase_09/fixtures/bandit_output.json contains `"results"` key with 4 entries
    - `python -m pytest tests/phase_09/ -x -q` exits 0 (all gosec + bandit tests pass)
  </acceptance_criteria>
  <done>BanditAdapter passes all unit tests: parses 4 findings, applies confidence x severity matrix correctly (HIGH/HIGH->CRITICAL, HIGH/MEDIUM->HIGH, MEDIUM/HIGH->MEDIUM, LOW/LOW->INFO), normalizes paths, handles exit codes, produces fingerprints.</done>
</task>

</tasks>

<verification>
All Phase 09 tests pass:
```bash
python -m pytest tests/phase_09/ -x -q
```

Adapter classes are importable:
```bash
python -c "from scanner.adapters.gosec import GosecAdapter; print(GosecAdapter().tool_name)"
python -c "from scanner.adapters.bandit import BanditAdapter; print(BanditAdapter().tool_name)"
```
</verification>

<success_criteria>
- GosecAdapter and BanditAdapter both subclass ScannerAdapter
- All 16+ unit tests pass (8 per adapter)
- gosec: direct severity mapping (HIGH/MEDIUM/LOW), string line number conversion, line_end == line_start
- Bandit: confidence x severity matrix, line_range support for multi-line findings
- Both: exit code 1 = findings found, exit code 2+ = error, 64-char fingerprints, path normalization
</success_criteria>

<output>
After completion, create `.planning/phases/09-tier-1-scanner-adapters/09-01-SUMMARY.md`
</output>
