---
phase: 11-cargo-audit-fix-and-documentation-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scanner/adapters/cargo_audit.py
  - tests/phase_09/test_adapter_cargo_audit.py
  - tests/phase_09/test_config_registration.py
  - tests/phase_11/__init__.py
  - tests/phase_11/test_cargo_audit_config_lookup.py
  - docs/en/admin-guide.md
  - docs/ru/admin-guide.md
  - docs/fr/admin-guide.md
  - docs/es/admin-guide.md
  - docs/it/admin-guide.md
  - Makefile
autonomous: true
requirements:
  - SCAN-04

must_haves:
  truths:
    - "Scanning a Rust project with cargo-audit completes without KeyError"
    - "admin-guide run() signature matches actual ScannerAdapter base class in all 5 languages"
    - "Makefile verify-scanners describes 12 scanners (11 binaries)"
  artifacts:
    - path: "src/scanner/adapters/cargo_audit.py"
      provides: "Fixed tool_name property returning cargo_audit"
      contains: 'return "cargo_audit"'
    - path: "tests/phase_11/test_cargo_audit_config_lookup.py"
      provides: "Integration test verifying orchestrator config lookup"
      contains: "settings.scanners[adapter.tool_name]"
    - path: "docs/en/admin-guide.md"
      provides: "Correct run() signature example"
      contains: "async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None)"
  key_links:
    - from: "src/scanner/adapters/cargo_audit.py"
      to: "config.yml"
      via: "tool_name property matching config key"
      pattern: 'return "cargo_audit"'
    - from: "src/scanner/core/orchestrator.py"
      to: "src/scanner/adapters/cargo_audit.py"
      via: "settings.scanners[adapter.tool_name] lookup"
      pattern: "settings\\.scanners\\[adapter\\.tool_name\\]"
---

<objective>
Fix the cargo-audit KeyError that prevents Rust project scanning, add an integration test proving config lookup works, correct the run() signature in admin-guide documentation across all 5 languages, and update Makefile scanner count wording.

Purpose: Close the remaining SCAN-04 gap so Rust dependency scanning works end-to-end and documentation accurately reflects the codebase.
Output: Fixed adapter, new integration test, corrected docs and Makefile.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cargo-audit-fix-and-documentation-corrections/11-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/scanner/adapters/base.py:
```python
class ScannerAdapter(abc.ABC):
    @property
    @abc.abstractmethod
    def tool_name(self) -> str: ...

    @abc.abstractmethod
    async def run(
        self,
        target_path: str,
        timeout: int,
        extra_args: list[str] | None = None,
    ) -> list[FindingSchema]: ...
```

From config.yml (line 88):
```yaml
  cargo_audit:       # <-- underscore, this is the key tool_name must match
    adapter_class: "scanner.adapters.cargo_audit.CargoAuditAdapter"
```

From src/scanner/core/orchestrator.py (line 196):
```python
settings.scanners[adapter.tool_name].timeout,    # KeyError when tool_name="cargo-audit"
settings.scanners[adapter.tool_name].extra_args or None,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cargo-audit tool_name and update existing tests</name>
  <files>
    src/scanner/adapters/cargo_audit.py,
    tests/phase_09/test_adapter_cargo_audit.py,
    tests/phase_09/test_config_registration.py,
    tests/phase_11/__init__.py,
    tests/phase_11/test_cargo_audit_config_lookup.py
  </files>
  <read_first>
    src/scanner/adapters/cargo_audit.py,
    src/scanner/adapters/base.py,
    src/scanner/core/orchestrator.py,
    config.yml,
    tests/phase_09/test_adapter_cargo_audit.py,
    tests/phase_09/test_config_registration.py,
    src/scanner/config.py
  </read_first>
  <action>
1. In `src/scanner/adapters/cargo_audit.py` line 95, change the `tool_name` property return value from `"cargo-audit"` to `"cargo_audit"`. Do NOT change `_version_command` -- the binary on disk is still called `cargo-audit` (hyphen), so the version command list must remain `["cargo-audit", "--version"]`. Also do NOT change the `cmd` list on line 116-121 which calls the binary `cargo-audit`.

2. In `tests/phase_09/test_config_registration.py` line 27, update the `NEW_SCANNERS` dict entry for `cargo_audit` to change `"tool_name": "cargo-audit"` to `"tool_name": "cargo_audit"`. Also line 83 in `test_registry_language_filtering` change `a.tool_name == "cargo-audit"` to `a.tool_name == "cargo_audit"`.

3. In `tests/phase_09/test_adapter_cargo_audit.py`, if any assertion checks `adapter.tool_name == "cargo-audit"`, change it to `"cargo_audit"`. Search the entire file for `"cargo-audit"` references used as tool_name checks (not binary command references).

4. Create `tests/phase_11/__init__.py` as empty file.

5. Create `tests/phase_11/test_cargo_audit_config_lookup.py` with an integration test that:
   - Imports `CargoAuditAdapter` and `ScannerToolConfig`
   - Creates a settings-like dict with key `"cargo_audit"` mapping to a `ScannerToolConfig`
   - Instantiates `CargoAuditAdapter()`
   - Asserts `adapter.tool_name == "cargo_audit"`
   - Asserts `settings["cargo_audit"]` is accessible via `settings[adapter.tool_name]` (i.e., the lookup does not raise KeyError)
   - Asserts `settings[adapter.tool_name].timeout == 60` to confirm correct config retrieved
   - Follow the pattern from `tests/phase_09/test_config_registration.py`
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && python -m pytest tests/phase_11/test_cargo_audit_config_lookup.py tests/phase_09/test_config_registration.py tests/phase_09/test_adapter_cargo_audit.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/scanner/adapters/cargo_audit.py` contains `return "cargo_audit"` (underscore, no hyphen) in the tool_name property
    - `src/scanner/adapters/cargo_audit.py` still contains `"cargo-audit"` in `_version_command` return value (binary name unchanged)
    - `src/scanner/adapters/cargo_audit.py` still contains `"cargo-audit"` in cmd list for audit execution (binary name unchanged)
    - `tests/phase_09/test_config_registration.py` contains `"tool_name": "cargo_audit"` (not `"cargo-audit"`)
    - `tests/phase_09/test_config_registration.py` contains `a.tool_name == "cargo_audit"` (not `"cargo-audit"`)
    - `tests/phase_11/test_cargo_audit_config_lookup.py` exists and contains `settings[adapter.tool_name]`
    - `tests/phase_11/test_cargo_audit_config_lookup.py` contains `assert adapter.tool_name == "cargo_audit"`
    - All three test files pass: `pytest tests/phase_11/ tests/phase_09/test_config_registration.py tests/phase_09/test_adapter_cargo_audit.py` exits 0
  </acceptance_criteria>
  <done>CargoAuditAdapter.tool_name returns "cargo_audit" matching config.yml key, all existing tests updated and passing, new integration test proves config lookup works without KeyError</done>
</task>

<task type="auto">
  <name>Task 2: Fix admin-guide run() signature and Makefile wording</name>
  <files>
    docs/en/admin-guide.md,
    docs/ru/admin-guide.md,
    docs/fr/admin-guide.md,
    docs/es/admin-guide.md,
    docs/it/admin-guide.md,
    Makefile
  </files>
  <read_first>
    docs/en/admin-guide.md,
    docs/ru/admin-guide.md,
    docs/fr/admin-guide.md,
    docs/es/admin-guide.md,
    docs/it/admin-guide.md,
    Makefile,
    src/scanner/adapters/base.py
  </read_first>
  <action>
1. In ALL 5 admin-guide.md files (en, ru, fr, es, it), find the "Adding a New Scanner" code example at line 138. Replace the incorrect signature:
   ```python
   async def run(self, target_path: str, config: dict) -> list[dict]:
   ```
   with the correct signature matching `ScannerAdapter` base class:
   ```python
   async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:
   ```
   Also update the return type import -- add `from scanner.schemas.finding import FindingSchema` to the example imports if not present. The example block starts around line 133-142. The full corrected example should be:
   ```python
   # src/scanner/adapters/my_scanner.py
   from scanner.adapters.base import ScannerAdapter
   from scanner.schemas.finding import FindingSchema

   class MyScannerAdapter(ScannerAdapter):
       async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:
           # Execute scanner binary and parse output
           ...
           return findings
   ```

2. In `Makefile` line 39, change:
   ```
   verify-scanners: ## Smoke-test all 11 scanner binaries inside Docker
   ```
   to:
   ```
   verify-scanners: ## Smoke-test 12 scanners (11 binaries) inside Docker
   ```

3. In `Makefile` line 40, change:
   ```
   @echo "Verifying 11 scanner binaries (Enlightn uses artisan, not a standalone binary)..."
   ```
   to:
   ```
   @echo "Verifying 12 scanners (11 binaries; Enlightn uses artisan, not a standalone binary)..."
   ```
  </action>
  <verify>
    <automated>cd /home/alex/naveksoft-security && grep -n "async def run" docs/*/admin-guide.md && grep -n "verify-scanners" Makefile | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `docs/en/admin-guide.md` contains `async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:`
    - `docs/ru/admin-guide.md` contains `async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:`
    - `docs/fr/admin-guide.md` contains `async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:`
    - `docs/es/admin-guide.md` contains `async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:`
    - `docs/it/admin-guide.md` contains `async def run(self, target_path: str, timeout: int, extra_args: list[str] | None = None) -> list[FindingSchema]:`
    - None of the 5 admin-guide files contain `config: dict` in the run() signature
    - All 5 admin-guide files contain `from scanner.schemas.finding import FindingSchema`
    - `Makefile` contains `## Smoke-test 12 scanners (11 binaries) inside Docker`
    - `Makefile` contains `Verifying 12 scanners (11 binaries;`
    - `Makefile` does NOT contain `all 11 scanner binaries`
  </acceptance_criteria>
  <done>All 5 admin-guide.md files show correct run() signature matching ScannerAdapter base class, Makefile describes "12 scanners (11 binaries)"</done>
</task>

</tasks>

<verification>
1. All tests pass: `python -m pytest tests/phase_09/ tests/phase_11/ -v`
2. The tool_name mismatch is resolved: `grep 'return "cargo_audit"' src/scanner/adapters/cargo_audit.py`
3. No doc files have the old signature: `grep -r 'config: dict' docs/*/admin-guide.md` returns empty
4. Makefile updated: `grep '12 scanners' Makefile`
</verification>

<success_criteria>
- cargo-audit adapter tool_name matches config.yml key (both "cargo_audit" with underscore)
- Integration test in tests/phase_11/ proves settings[adapter.tool_name] lookup works
- All existing phase_09 tests still pass after tool_name change
- All 5 admin-guide.md files have correct run() signature
- Makefile describes 12 scanners (11 binaries)
</success_criteria>

<output>
After completion, create `.planning/phases/11-cargo-audit-fix-and-documentation-corrections/11-01-SUMMARY.md`
</output>
