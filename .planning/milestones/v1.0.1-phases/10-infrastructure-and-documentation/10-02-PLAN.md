---
phase: 10-infrastructure-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/en/user-guide.md
  - docs/en/admin-guide.md
  - docs/en/devops-guide.md
  - docs/en/architecture.md
  - docs/en/api.md
  - docs/en/custom-rules.md
  - docs/en/database-schema.md
  - docs/en/transfer-guide.md
  - README.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "English user-guide lists all 12 scanners with per-scanner cards"
    - "English admin-guide has Plugin Registry section with adapter_class docs"
    - "English devops-guide documents all 12 scanner binaries in Docker"
    - "All English docs reference 12 scanners, not 5"
    - "README.md references 12 scanners and plugin registry"
  artifacts:
    - path: "docs/en/user-guide.md"
      provides: "12 scanner cards"
      contains: "gosec"
    - path: "docs/en/admin-guide.md"
      provides: "Plugin Registry section"
      contains: "adapter_class"
    - path: "docs/en/devops-guide.md"
      provides: "Docker binary docs for 12 scanners"
      contains: "cargo-audit"
    - path: "README.md"
      provides: "Updated scanner count"
      contains: "12"
  key_links:
    - from: "docs/en/admin-guide.md"
      to: "config.yml"
      via: "adapter_class documentation"
      pattern: "adapter_class.*scanner\\.adapters"
---

<objective>
Update all 8 English documentation files and README.md to reflect 12 scanners and the plugin registry architecture.

Purpose: Create the English source content for DOCS-01 that translated docs (Plan 03) will mirror. All scanner counts, scanner cards, plugin registry docs, and Docker references must be current.
Output: 9 updated English files (8 docs + README).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-infrastructure-and-documentation/10-CONTEXT.md
@.planning/phases/10-infrastructure-and-documentation/10-RESEARCH.md
@config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update user-guide.md, admin-guide.md, and devops-guide.md (English)</name>
  <files>docs/en/user-guide.md, docs/en/admin-guide.md, docs/en/devops-guide.md</files>
  <read_first>docs/en/user-guide.md, docs/en/admin-guide.md, docs/en/devops-guide.md, config.yml, .planning/phases/10-infrastructure-and-documentation/10-CONTEXT.md</read_first>
  <action>
**user-guide.md** -- Major update:
1. Change opening description from "five parallel static analysis tools" to "twelve parallel security scanning tools" (or "12 security scanning tools").
2. Add a new section `## Supported Scanners` after the opening paragraph with per-scanner cards for ALL 12 scanners. Each card format:
```markdown
### Scanner-Name (Language)

**Language:** X
**Type:** SAST/SCA/Secrets
**What it detects:** Brief description
**Example finding:**
> RULE-ID: Description at `file.ext:line`

**Enabled:** Automatically when X files are detected / Always enabled
```

The 12 scanners to document (use config.yml for languages):
- Semgrep (Multi-language SAST) -- python, php, javascript, typescript, go, java, kotlin, ruby, csharp, rust
- Cppcheck (C/C++) -- cpp
- Gitleaks (Secrets) -- all languages (languages: [])
- Trivy (Infrastructure) -- docker, terraform, yaml
- Checkov (Infrastructure) -- docker, terraform, yaml, ci
- Psalm (PHP) -- php
- Enlightn (Laravel) -- laravel
- PHP Security Checker (PHP SCA) -- php
- gosec (Go SAST) -- go. Detects: hardcoded credentials, SQL injection, insecure crypto, unsafe file permissions. Example: `G101: Potential hardcoded credentials`
- Bandit (Python SAST) -- python. Detects: hardcoded passwords, SQL injection, eval usage, weak crypto. Example: `B105: Possible hardcoded password`
- Brakeman (Ruby/Rails SAST) -- ruby. Detects: SQL injection, XSS, mass assignment, command injection. Example: `SQL Injection: Possible SQL injection near line 15`
- cargo-audit (Rust SCA) -- rust. Detects: known vulnerable dependencies via RustSec advisory database. Example: `RUSTSEC-2019-0009: heap overflow in smallvec`

**admin-guide.md** -- Major update:
1. Change "five scanner tools" to "twelve scanner tools" in the Scanner Configuration section.
2. Update the YAML example to show ALL 12 scanners (copy structure from config.yml but show only key fields: enabled, timeout, languages, adapter_class).
3. Add new section `## Plugin Registry` after Scanner Configuration with:
   - How scanners are registered via config.yml `adapter_class` field
   - How to add a new scanner (create adapter class implementing ScannerAdapter, add entry to config.yml scanners section with adapter_class path)
   - The `adapter_class` field format: `scanner.adapters.module_name.ClassName`
   - Language auto-detection: scanners with `languages` field are auto-enabled when matching files detected
   - The `/api/scanners` endpoint for listing registered scanners
   - Example config entry for a hypothetical new scanner

**devops-guide.md** -- Moderate update:
1. Update any references to scanner count from 5 to 12.
2. Add/update a section `## Scanner Binaries` or similar listing all 12 tools installed in Docker:
   - semgrep (pip), cppcheck (apt), gitleaks (binary), trivy (binary), checkov (pip), psalm (composer), enlightn (composer), php-security-checker (binary), gosec (binary), bandit (pip), brakeman (ruby gem), cargo-audit (binary)
3. Document the `make verify-scanners` target for smoke-testing scanner availability.
4. Update multi-arch build notes to mention all new binary downloads support amd64/arm64.
  </action>
  <verify>
    <automated>grep -c "gosec\|Bandit\|Brakeman\|cargo-audit" docs/en/user-guide.md && grep "adapter_class" docs/en/admin-guide.md && grep "verify-scanners" docs/en/devops-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/en/user-guide.md contains "twelve" or "12" (not "five") when describing scanner count
    - docs/en/user-guide.md contains scanner card sections for gosec, Bandit, Brakeman, cargo-audit
    - docs/en/user-guide.md contains `## Supported Scanners` section
    - docs/en/admin-guide.md contains `## Plugin Registry` section
    - docs/en/admin-guide.md contains `adapter_class` at least 3 times (in explanation + examples)
    - docs/en/admin-guide.md shows all 12 scanners in YAML config example
    - docs/en/admin-guide.md contains `/api/scanners` endpoint reference
    - docs/en/devops-guide.md contains `verify-scanners` reference
    - docs/en/devops-guide.md mentions gosec, bandit, brakeman, cargo-audit in Docker/binary section
    - None of the 3 files contains "five scanner" (case-insensitive)
  </acceptance_criteria>
  <done>user-guide has 12 scanner cards, admin-guide has Plugin Registry section with adapter_class docs, devops-guide documents all scanner binaries and verify-scanners target.</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining 5 English docs and README.md</name>
  <files>docs/en/architecture.md, docs/en/api.md, docs/en/custom-rules.md, docs/en/database-schema.md, docs/en/transfer-guide.md, README.md</files>
  <read_first>docs/en/architecture.md, docs/en/api.md, docs/en/custom-rules.md, docs/en/database-schema.md, docs/en/transfer-guide.md, README.md</read_first>
  <action>
**architecture.md:**
- Update scanner count references from 5 to 12
- If there is a mermaid/ASCII diagram showing adapters, add gosec, bandit, brakeman, cargo-audit nodes
- Update any text describing "five adapters" or listing adapter classes
- Add reference to plugin registry (ScannerRegistry loads adapters from config.yml)

**api.md:**
- Add `/api/scanners` endpoint documentation if not already present: GET /api/scanners returns list of registered scanners with name, enabled status, languages. Requires X-API-Key header.
- Update any scanner count references from 5 to 12
- Update any example responses that list scanner names to include all 12

**custom-rules.md:**
- Update scanner references if present (likely minimal changes)
- If it lists scanners that support custom rules, add: gosec supports custom rules via `-include` flag, Bandit supports custom profiles, Brakeman supports `-t` type filtering
- Update count references

**database-schema.md:**
- Update scanner name references if any table or example shows scanner names
- Likely minimal changes -- update any "five" references to "twelve"

**transfer-guide.md:**
- Update scanner count references
- If it lists scanner binaries to install, add gosec, bandit, brakeman, cargo-audit
- Update any migration/transfer checklist to include new scanner verification

**README.md:**
- Update scanner count from current value to 12
- Update features list to mention plugin registry architecture
- Add gosec, Bandit, Brakeman, cargo-audit to the list of supported scanners
- Update any "five scanners" or "5 scanners" text
- Mention Go, Rust in supported languages if not already listed
- Keep the language selector links at top (EN, RU, FR, ES, IT) -- they should already be present
  </action>
  <verify>
    <automated>grep -il "five scanner\|5 scanner" docs/en/architecture.md docs/en/api.md docs/en/custom-rules.md docs/en/database-schema.md docs/en/transfer-guide.md README.md 2>/dev/null; echo "exit:$?"; grep "12" README.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/en/architecture.md does not contain "five scanner" (case-insensitive)
    - docs/en/architecture.md mentions gosec, bandit, brakeman, cargo-audit (in diagram or text)
    - docs/en/api.md contains `/api/scanners` endpoint documentation
    - docs/en/custom-rules.md does not contain "five scanner" (case-insensitive)
    - docs/en/database-schema.md does not contain "five scanner" (case-insensitive)
    - docs/en/transfer-guide.md does not contain "five scanner" (case-insensitive)
    - README.md contains "12" in reference to scanner count
    - README.md mentions gosec, Bandit, Brakeman, cargo-audit
    - `grep -ril "five scanner" docs/en/ README.md` returns no results
  </acceptance_criteria>
  <done>All 8 English doc files and README.md consistently reference 12 scanners, plugin registry, and all scanner names.</done>
</task>

</tasks>

<verification>
- `grep -ril "five scanner" docs/en/ README.md` returns empty (no stale references)
- `grep -l "gosec" docs/en/user-guide.md docs/en/admin-guide.md docs/en/devops-guide.md docs/en/architecture.md` returns all 4 files
- `grep "adapter_class" docs/en/admin-guide.md` returns matches
- `grep "12" README.md` returns matches
- `grep "/api/scanners" docs/en/api.md` returns matches
</verification>

<success_criteria>
- All 8 English docs reference 12 scanners consistently
- user-guide has per-scanner cards for all 12 scanners
- admin-guide has Plugin Registry section with adapter_class documentation
- devops-guide documents Docker scanner binaries and verify-scanners target
- README.md updated with 12 scanners and plugin registry mention
- No stale "five scanner" references in any English doc
</success_criteria>

<output>
After completion, create `.planning/phases/10-infrastructure-and-documentation/10-02-SUMMARY.md`
</output>
